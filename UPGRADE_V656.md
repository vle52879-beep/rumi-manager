# RUMI v6.5.6

- Sửa lỗi sidebar hiện đè lên màn hình đăng nhập do CSS `:has(.studio-auth)` ghi đè class `hidden`.
- Đảm bảo `#auth-root.hidden` và `#app-root.hidden` luôn ẩn đúng.
- Tắt lớp hiệu ứng cũ của `v6.js` trên màn hình đăng nhập mới để chỉ còn một ly trà sữa.
